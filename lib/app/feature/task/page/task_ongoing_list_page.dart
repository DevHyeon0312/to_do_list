import 'package:flutter/material.dart';

class TaskOngoingListPage extends StatelessWidget {
  const TaskOngoingListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
