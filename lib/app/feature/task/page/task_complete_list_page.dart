import 'package:flutter/material.dart';

class TaskCompleteListPage extends StatelessWidget {
  const TaskCompleteListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
